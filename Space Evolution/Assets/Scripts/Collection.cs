using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collection : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		// When the player is near enough to a broken off piece of an asteroid, they collect it and it is added to their inventory
	}
}
