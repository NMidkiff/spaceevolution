using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Evolution : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		// On the press of the inventory button, a UI is opened up
        // The player can see the materials they have collected 
        // The player gets a view of their ship where they can upgrade
        // They can then select to add new guns, add new engines, increase size, and later, to upgrade each of those components or alter them for different effects
	}
}
